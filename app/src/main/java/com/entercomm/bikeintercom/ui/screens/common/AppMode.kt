package com.entercomm.bikeintercom.ui.screens.common

/**
 * App state that drives all animations cohesively
 */
enum class AppMode {
    INITIALIZING,
    STANDBY,
    CONNECTING,
    ACTIVE,
    TRANSMITTING,
}
